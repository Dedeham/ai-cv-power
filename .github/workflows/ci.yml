name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate-protocol:
    name: Validate protocol foundation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static protocol checks
        run: bash scripts/validate-protocol.sh
